<%= form_with(model: admincontrol, local: true) do |form| %>
  <% if admincontrol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admincontrol.errors.count, "error") %> prohibited this admincontrol from being saved:</h2>

      <ul>
        <% admincontrol.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
